# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 |  | architecture | Backend architecture pattern | Hexagonal (ports & adapters) architecture on Quarkus | Keeps domain logic framework-independent and testable through ports. Domain models are clean POJOs; Panache entities live in infrastructure as adapters. User's explicit preference. |  | human |
| D002 |  | architecture | Session management ownership | SvelteKit owns the session via signed HTTP-only cookie; Quarkus is a trusted internal API | Single auth layer, no CORS complexity, cookie never touches browser JS. Quarkus stays a pure API without session management concerns. |  | collaborative |
| D003 |  | library | Dashboard charting library | Layerchart via shadcn-svelte Chart component | Svelte-native, built on D3, integrates with Tailwind and shadcn design language. Already bundled with shadcn-svelte — no extra dependency needed. |  | collaborative |
| D004 |  | architecture | Authentication mechanism for M001 | Simple password auth with hashed credential in DB; passkeys/WebAuthn deferred to M002 | Reduces M001 complexity. Single user with a strong password behind HTTPS is sufficient. WebAuthn ceremony and credential storage can wait. |  | human |
| D005 |  | pattern | Debt payment income tracking | Debt payments auto-create corresponding ingress transactions | Eliminates manual double-entry — record the payment once, the system handles the accounting. Dashboard income figures stay accurate automatically. |  | collaborative |
| D006 |  | pattern | Subscription billing record generation | Quarkus @Scheduled daily job generates pending payment records 7 days before billing date | Members see upcoming dues on public view, unpaid periods accumulate visibly. Job checks billing dates (not time since last run) so restarts are safe. |  | collaborative |
| D007 |  | architecture | Frontend-backend communication pattern | SvelteKit server routes proxy all requests to Quarkus; browser never talks to Quarkus directly | Cleaner for auth (session cookie stays server-side), single domain, no CORS issues. Quarkus becomes a pure internal API. |  | human |
| D008 |  | architecture | Backend Java package naming and hexagonal structure | com.keenti.finances with domain/{model,port/{in,out}}, application/service, infrastructure/adapter/{in/rest,out/{persistence,security}} layout | Follows standard hexagonal architecture conventions. Separates domain (framework-free POJOs and port interfaces), application (use case orchestration), and infrastructure (Panache entities, REST resources, bcrypt). Enables testability and framework independence per R015. |  | agent |
| D009 |  | architecture | SvelteKit session management implementation | HMAC-SHA256 signed HTTP-only cookie via Node crypto, with SESSION_SECRET env var and dev fallback | Implements the 'SvelteKit owns the session' architectural decision from M001-CONTEXT. HTTP-only prevents XSS access, HMAC signing prevents forgery, SameSite=Lax prevents CSRF. Dev fallback allows local development without env setup. Single-user app means cookie payload is just the username. |  | agent |
| D010 |  | architecture | SvelteKit-to-Quarkus API proxy pattern | Catch-all +server.ts at /api/[...path] forwards all HTTP methods to http://localhost:8080/api/{path}. Quarkus is never exposed to the browser. | Single origin eliminates CORS complexity. All auth happens at the SvelteKit layer. Quarkus stays a pure internal API. The catch-all pattern scales to all future API routes without per-endpoint proxy files. |  | agent |
| D011 |  | architecture | Railway deployment topology for Quarkus + SvelteKit monorepo | Two Railway services (backend, frontend) from same repo with separate root directories and Dockerfiles, plus one Railway PostgreSQL plugin. Frontend connects to backend via Railway's private networking (BACKEND_URL env var). No nginx/reverse proxy — Railway handles HTTPS termination and routing. | Matches Railway's native monorepo pattern. Dockerfiles give deterministic builds (vs Nixpacks auto-detection). Private networking keeps Quarkus unexposed to the internet — only SvelteKit proxy reaches it. User has prior Railway deployment experience with this stack. |  | agent |
| D012 |  | navigation | App navigation pattern for M002 | macOS-style centered bottom dock with static icons; desktop shows all items, mobile shows 3 pinned (Transactions, Subscriptions, Debt) + overflow menu dialog | User wants to move away from sidebar. Dock feels more personal and compact. Static icons keep implementation simple. Mobile pinning ensures most-used items are always one tap away. | Yes | human |
| D013 |  | theming | Theme switching mechanism for M002 | prefers-color-scheme detection via inline script in app.html that sets .dark on html before first paint; no manual toggle, pure system-follow | Existing OKLCH variable sets already cover both modes. Inline script prevents flash. System-follow is sufficient for single-user app on Apple devices. | Yes | collaborative |
| D014 |  | data-model | Category color storage approach | color varchar column storing OKLCH hue value; frontend renders badges with fixed lightness/chroma per theme; curated swatch picker constrained by direction (green-ish INGRESS, red-complement EGRESS, blue-ish BOTH) | Storing just the hue keeps palette consistent and theme-adaptive. Direction constraint preserves financial semantics. User picks from curated palette rather than auto-assignment. | Yes | collaborative |
| D015 |  | data-model | Subscription owner participation model | Boolean owner_participates column (default true) on subscription table; billing splits cost/(memberCount + ownerParticipates?1:0); no payment record for owner when false (middleman mode) | User is sometimes participant, sometimes middleman. Boolean flag cleanly captures both modes without complicating the member management model. | Yes | collaborative |
| D016 |  | architecture | Auth migration from password to passkeys for M002 | WorkOS AuthKit with @workos/authkit-sveltekit; passkey-only login; WorkOS manages sessions instead of HMAC cookie | WorkOS free tier handles WebAuthn ceremony, session management, and passkey storage. Simpler than custom WebAuthn. Passkey-only is cleaner for single-user app. Replaces D002/D004/D009. | Yes | collaborative |
| D017 |  | data-model | Transaction-subscription linking approach | Nullable subscription_id FK on transaction table; retroactive tagging via multi-select with inline previews on subscription detail page; atomic linking; no synthetic debt creation | User is migrating historical data. Retroactive FK tagging is simpler than synthetic record creation. Inline previews prevent context-switching during linking. No debt linking needed since debts are tracked by subscription structure. | Yes | collaborative |
| D018 |  | UI | Mobile layout strategy for list pages | Card-based list views at ≤768px breakpoint for transactions, subscriptions, debts; tap navigates to detail view with action buttons; desktop table views unchanged | Tables don't compress well on narrow viewports. Cards preserve information-at-a-glance promise. Detail view with actions keeps card surfaces clean. User explicitly wants a rethink, not responsive tables with horizontal scroll. | Yes | collaborative |
| D019 |  | architecture | Dock component structure and mobile overflow pattern | Single Dock component with inline overflow dialog. Desktop: all icons visible in centered bottom bar. Mobile: 3 pinned (Transactions, Subscriptions, Debts) + EllipsisVertical button opening shadcn Dialog with remaining items. Sidebar deleted entirely. | Reuses existing shadcn Dialog component. Three pinned items chosen because they represent the primary user loops (viewing transactions, managing subscriptions, tracking debts). Dashboard/Categories/Contacts are secondary and fit in overflow. Single component file + dialog keeps the navigation self-contained. | yes — pinned items could be reconfigured if usage patterns differ | agent |
| D020 |  | library | Which WorkOS package to use for SvelteKit passkey auth | Use @workos-inc/node SDK directly with manual OAuth/PKCE flow, not @workos-inc/authkit-sveltekit (which does not exist on npm) | The research assumed @workos-inc/authkit-sveltekit existed but it does not. WorkOS only publishes framework-specific AuthKit for Next.js. For SvelteKit we must use the generic Node SDK and implement the authorization code exchange, session cookie encryption, and refresh flow manually in SvelteKit hooks and routes. | yes — if WorkOS publishes a SvelteKit AuthKit package in the future | agent |
| D021 |  | library | Whether to keep or remove unused layerchart dependency | Remove layerchart from frontend/package.json — it has zero imports in src/ and is dead weight | layerchart v1.0.13 was added anticipating dashboard charts but never imported. Keeping it adds bundle/install overhead and a false Svelte 5 compatibility concern (R012). Re-adding later is trivial if charting is needed. | yes — re-add if charting features are planned in a future milestone | agent |
| D022 |  | architecture | Multi-user security approach for M003 | Monolith with query-scoped security via Hibernate @Filter (user_id + soft-delete), not microservices | PostgreSQL MVCC handles concurrent users natively. 30 users is trivial load. Hexagonal architecture funnels all data access through repository ports — user-scope filtering at that layer gives tenant isolation without distributed transactions, inter-service auth, or operational overhead. Microservices rejected: same WHERE clause needed regardless, plus added cost and complexity. | Yes | collaborative |
| D023 |  | architecture | User-scoping and soft-delete enforcement mechanism | Hibernate @FilterDef with two stacked filters: userScope (user_id = :currentUser) and softDelete (deleted_at IS NULL), enabled per-session in each repository | Defense-in-depth: queries can't accidentally return cross-user or deleted rows. Same pattern for both concerns, natural to stack. More robust than manual WHERE clauses where one missed clause is a data leak. Explicit WHERE alternative was rejected for this reason. | Yes | collaborative |
| D024 |  | architecture | User identity propagation from SvelteKit to Quarkus | SvelteKit proxy injects X-WorkOS-User-Id header; Quarkus UserContext @RequestScoped bean resolves to local app_user.id via workos_id column; JIT provisioning on first request from unknown user | Preserves 'SvelteKit owns auth' decision. Quarkus trusts header because only reachable via proxy (Railway private networking). Local app_user provides stable numeric FK and home for preferences. JWT validation in Quarkus rejected as unnecessary double-auth. | Yes | collaborative |
| D025 |  | frontend | Category color picker expansion for M003 | Full 360 degree hue wheel plus hex input converting to OKLCH hue; badge rendering keeps fixed lightness/chroma per theme; live preview in both light and dark modes | Maximum personalization while keeping theme-adaptive rendering. Hex input is familiar UX; OKLCH hue conversion ensures badges look good in both themes. Full hex storage rejected because fixed colors break contrast in one theme. Direction constraints dropped per user preference for full freedom. | Yes | collaborative |
| D026 |  | frontend | Font loading strategy for per-user typography presets | Preload all shadcn-svelte recommended font presets (~4-6 fonts) in app.html; toggle active fonts via CSS variables per user preference | Small font set makes weight negligible. Preloading eliminates FOUT on first visit. Instant font switching without network requests. Dynamic link injection rejected for visible FOUT. | Yes | collaborative |
| D027 |  | data-model | User preferences storage location | Store primary_hue, heading_font, body_font as columns directly on app_user table | Three columns don't warrant a separate table. Loaded once per session alongside user identity. Avoids extra JOIN. Separate user_preferences table rejected as unnecessary indirection. | Yes | collaborative |
| D028 |  | architecture | How to inject X-WorkOS-User-Id into all SvelteKit-to-Quarkus requests | Use SvelteKit handleFetch hook to inject X-WorkOS-User-Id header into all backend-bound requests, rather than modifying each page.server.ts individually. The proxy and direct fetch calls both pass through handleFetch. | Page.server.ts files make direct backend calls (not through the proxy route). Modifying each file individually is error-prone — handleFetch intercepts all server-side fetch calls and can add the header centrally. The proxy route also benefits since its fetch calls pass through handleFetch. | Yes | agent |
| D029 |  | architecture | How to enforce user-scope filter on non-public Quarkus endpoints | JAX-RS ContainerRequestFilter (@Provider) enables Hibernate userScope filter for all requests with X-WorkOS-User-Id header. Requests without the header to non-public paths return 401. PublicSubscriptionResource and SubscriptionBillingScheduler naturally bypass (no header, no HTTP context). | Single enforcement point prevents data leaks from missed WHERE clauses. Public endpoint and scheduler naturally bypass because no header is present and filter is not enabled. Defense-in-depth: ContainerRequestFilter + Hibernate @Filter + repository-level user assignment on writes. | Yes | agent |
| D030 |  | architecture | User-scoping strategy for multi-tenant data isolation | Hibernate @FilterDef/@Filter with JAX-RS ContainerRequestFilter enablement. Single UserScopeFilter provider reads X-WorkOS-User-Id header, resolves local user via JIT provisioning, enables userScope filter on Session. Native SQL queries get manual WHERE user_id clauses (3 queries). | Centralized filter enablement covers all Panache queries automatically. Only 3 native SQL queries need manual WHERE clauses. Public endpoints and scheduled jobs naturally bypass filters (no request context = no filter enabled). Avoids repetitive WHERE clause additions across dozens of repository methods. | no — foundational to all M003 slices | agent |
| D031 |  | architecture | Where to store user_id for multi-user scoping | Direct user_id FK on 5 entities (Category, Contact, Transaction, Subscription, Debt). Child entities (SubscriptionMember, PaymentRecord, DebtPayment) inherit scoping through parent FK — no user_id column needed. Domain POJOs do NOT gain userId; scoping stays entirely in infrastructure layer. | Child entities are always accessed through their parent, so adding user_id would be redundant and create sync risk. Keeping userId out of domain POJOs maintains clean separation — repositories inject UserContext for writes, Hibernate filters handle reads. | Yes | agent |
| D032 |  | architecture | How to enable Hibernate @Filter for user-scoping across all Quarkus endpoints | JAX-RS ContainerRequestFilter (@Provider) that reads X-WorkOS-User-Id header, resolves/provisions local app_user, enables userScope Hibernate filter, and stores userId in @RequestScoped UserContext bean. Non-public endpoints without the header get 401. Public endpoints and scheduled jobs naturally bypass (no header = no filter). | Single enforcement point prevents data leaks from missed WHERE clauses. Centralized filter enablement covers all Panache queries automatically. Only 3 native SQL queries need manual WHERE user_id. PublicSubscriptionResource and SubscriptionBillingScheduler naturally bypass because no header/request context exists. | Yes | agent |
| D033 |  | architecture | Where to store user_id for multi-user data ownership | Direct user_id FK on 5 root entities (Category, Contact, Transaction, Subscription, Debt). Child entities (SubscriptionMember, PaymentRecord, DebtPayment) inherit scoping through parent FK. No user_id on domain POJOs — repositories inject UserContext for writes, Hibernate @Filter handles reads. | Child entities are always accessed through parents; adding user_id would be redundant. Keeping userId out of domain POJOs maintains clean hexagonal separation. Repository toEntity() methods set user on save/update. | Yes | agent |
| D034 |  | data-integrity | How to handle unique constraints on category.name when soft-deleted rows still exist | Partial unique index WHERE deleted_at IS NULL — soft-deleted categories release their name slot for reuse | A full UNIQUE (user_id, name) constraint would block users from recreating a category with the same name as a previously deleted one. Partial index WHERE deleted_at IS NULL allows name reuse after deletion while still preventing duplicates among active categories. PostgreSQL supports partial unique indexes natively. The alternative of appending timestamps to deleted names adds complexity and makes trash-view display messy. | yes — could switch to timestamp-suffix approach if partial index causes issues with H2 test DB | agent |
| D035 |  | architecture | How to handle category UNIQUE (user_id, name) constraint with soft deletes | Replace full UNIQUE constraint with partial unique index WHERE deleted_at IS NULL in V11 migration | A full UNIQUE (user_id, name) constraint prevents users from recreating a category with the same name as a soft-deleted one. Partial unique index releases the name slot when a category is soft-deleted, allowing reuse. This is the standard PostgreSQL pattern for soft-delete + uniqueness coexistence. | Yes | agent |
| D036 |  | architecture | Whether child entities (DebtPayment, SubscriptionMember) get independent soft-delete columns | No independent soft-delete on child entities — they inherit parent lifecycle via Hibernate filter on parent | When a Debt or Subscription is soft-deleted, its children (DebtPayment, SubscriptionMember) become invisible because the parent is filtered out. Restoring the parent restores child visibility automatically. Adding deleted_at to children would create orphan-restore complexity with no user-facing benefit. | Yes | agent |
